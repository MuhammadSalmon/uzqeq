import HeroSlider from '@/components/Hero';
import React from 'react';

const HomePage = () => {
    return (
        <main>
            <HeroSlider/>
        </main>
    );
};

export default HomePage;